import { Meta, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from '.';

<Meta title="Buttons" component={Button} />

# Button
### Colors
Buttons can be colorized by adding `color` tag to the component.
`blue` is our primary (brand) color. `gray` or `white` should always be used as a
secondary action button. `red` should only ever be used for deleting
or something critical (dangerous).

<Story name="Button">
  ### Button
  <Preview>
    <Button color="blue">Blue Button</Button>
    <Button color="gray">Gray Button</Button>
    <Button color="white">White Button</Button>
    <Button color="red">Red Button</Button>
  </Preview>

  ### Sizes
  Buttons can be sized by adding the `size` tag to the component. Sizes come in
  `xs`, `sm`, `lg`, `xl`.  No size specified is our default size.
  <Preview>
    <Button color="blue" size="xs">XS Button</Button>
    <Button color="blue" size="sm">Small Button</Button>
    <Button color="blue">Button</Button>
    <Button color="blue" size="lg">Large Button</Button>
    <Button color="blue" size="xl">XL Button</Button>
  </Preview>

  ### Block
  To fill the width of a card or a form, simply add `className="w-full"` to the `Button`
  component.
  <Preview>
    <Button color="blue" className="w-full">Button</Button>
  </Preview>

  ### States
  Buttons can be disabled by adding `disabled="true"`.
  component.
  <Preview>
    <Button color="blue" disabled="true">Disabled Button</Button>
  </Preview>
</Story>



### Properties
<Props of={Button} />
